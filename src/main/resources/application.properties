spring.application.name=Hugbunadarverkefni1
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update

# Default profile uses H2 for local dev.
# On Render, SPRING_PROFILES_ACTIVE=prod loads application-prod.properties
# which points to the PostgreSQL database.
