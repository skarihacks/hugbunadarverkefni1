package is.hi.hbv501g.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Placeholder for future web configuration (e.g., CORS, formatters).
}
