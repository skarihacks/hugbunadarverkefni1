package is.hi.hbv501g.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Basic placeholder configuration to keep the application context explicit.
}
